import { Container, styled } from '@mui/material';

export const TodoContainerStyled = styled(Container)(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: '10px 0',
  marginTop: '10px'
}));
